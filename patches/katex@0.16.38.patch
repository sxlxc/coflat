diff --git a/dist/katex.js b/dist/katex.js
index 69a5eac63dba04361390b46275880332c983cc9f..caa994ca30335290472ec41e19fc3167951bf3d0 100644
--- a/dist/katex.js
+++ b/dist/katex.js
@@ -1265,6 +1265,7 @@ class SymbolNode {
     this.width = width || 0;
     this.classes = classes || [];
     this.style = style || {};
+    this.attributes = {};
     this.maxFontSize = 0;
 
     // Mark text from non-Latin scripts with specific classes so that we
@@ -1286,6 +1287,9 @@ class SymbolNode {
   hasClass(className) {
     return this.classes.includes(className);
   }
+  setAttribute(attribute, value) {
+    this.attributes[attribute] = value;
+  }
 
   /**
    * Creates a text node or span from a symbol node. Note that a span is only
@@ -1306,6 +1310,10 @@ class SymbolNode {
       span = span || document.createElement("span");
       span.style[key] = this.style[key];
     }
+    for (const attr of Object.keys(this.attributes)) {
+      span = span || document.createElement("span");
+      span.setAttribute(attr, this.attributes[attr]);
+    }
     if (span) {
       span.appendChild(node);
       return span;
@@ -1339,6 +1347,10 @@ class SymbolNode {
       needsSpan = true;
       markup += " style=\"" + utils_escape(styles) + "\"";
     }
+    for (const attr of Object.keys(this.attributes)) {
+      needsSpan = true;
+      markup += " " + attr + "=\"" + utils_escape(this.attributes[attr]) + "\"";
+    }
     const escaped = utils_escape(this.text);
     if (needsSpan) {
       markup += ">";
@@ -4987,6 +4999,17 @@ const makeOrd = function (group, options, type) {
  * correction so we don't lose spacing between combined glyphs.
  */
 const canCombine = (prev, next) => {
+  // Only combine mapped text when its original character boundaries survive.
+  const start = prev.attributes["data-loc-start"];
+  const end = prev.attributes["data-loc-end"];
+  const nextStart = next.attributes["data-loc-start"];
+  const nextEnd = next.attributes["data-loc-end"];
+  if (start != null || nextStart != null) {
+      if (end !== nextStart || +end - +start !== prev.text.length
+          || +nextEnd - +nextStart !== next.text.length) {
+          return false;
+      }
+  }
   if (createClass(prev.classes) !== createClass(next.classes) || prev.skew !== next.skew || prev.maxFontSize !== next.maxFontSize || prev.italic !== 0 && prev.hasClass("mathnormal")) {
     return false;
   }
@@ -5021,6 +5044,9 @@ const tryCombineChars = chars => {
     const prev = chars[i];
     const next = chars[i + 1];
     if (prev instanceof SymbolNode && next instanceof SymbolNode && canCombine(prev, next)) {
+      if (next.attributes["data-loc-end"] != null) {
+        prev.setAttribute("data-loc-end", next.attributes["data-loc-end"]);
+      }
       prev.text += next.text;
       prev.height = Math.max(prev.height, next.height);
       prev.depth = Math.max(prev.depth, next.depth);
@@ -5819,6 +5845,40 @@ const makeNullDelimiter = function (options, classes) {
   return makeSpan(classes.concat(moreClasses));
 };
 
+/**
+ * Recursively scan a virtual DOM node for data-loc-start / data-loc-end
+ * attributes and return the merged [min, max] range, or null if none found.
+ */
+function _inheritLocRange(node) {
+  let minStart = Infinity;
+  let maxEnd = -Infinity;
+  const stack = [node];
+  while (stack.length) {
+    const cur = stack.pop();
+    if (cur.attributes) {
+      const s = cur.attributes["data-loc-start"];
+      const e = cur.attributes["data-loc-end"];
+      if (s != null) {
+        const sv = +s;
+        if (sv < minStart) minStart = sv;
+      }
+      if (e != null) {
+        const ev = +e;
+        if (ev > maxEnd) maxEnd = ev;
+      }
+    }
+    if (cur.children) {
+      for (let i = cur.children.length - 1; i >= 0; i--) {
+        stack.push(cur.children[i]);
+      }
+    }
+  }
+  if (minStart !== Infinity && maxEnd !== -Infinity) {
+    return [minStart, maxEnd];
+  }
+  return null;
+}
+
 /**
  * buildGroup is the function that takes a group and calls the correct groupType
  * function for it. It also handles the interaction of size and style changes
@@ -5841,6 +5901,20 @@ const buildGroup = function (group, options, baseOptions) {
       groupNode.height *= multiplier;
       groupNode.depth *= multiplier;
     }
+    if ("setAttribute" in groupNode && typeof groupNode.setAttribute === "function" &&
+        (!groupNode.attributes || groupNode.attributes["data-loc-start"] == null)) {
+      const loc = group.loc && (group.loc.source || group.loc);
+      if (loc != null) {
+        groupNode.setAttribute("data-loc-start", String(loc.start));
+        groupNode.setAttribute("data-loc-end", String(loc.end));
+      } else {
+        const range = _inheritLocRange(groupNode);
+        if (range) {
+          groupNode.setAttribute("data-loc-start", String(range[0]));
+          groupNode.setAttribute("data-loc-end", String(range[1]));
+        }
+      }
+    }
     return groupNode;
   } else {
     throw new src_ParseError("Got group of unknown type: '" + group.type + "'");
@@ -9929,10 +10003,11 @@ class SourceLocation {
   // Start offset, zero-based inclusive.
   // End offset, zero-based exclusive.
 
-  constructor(lexer, start, end) {
+  constructor(lexer, start, end, source) {
     this.lexer = lexer;
     this.start = start;
     this.end = end;
+    this.source = source;
   }
 
   /**
@@ -9949,7 +10024,12 @@ class SourceLocation {
     } else if (!first || !first.loc || !second.loc || first.loc.lexer !== second.loc.lexer) {
       return null;
     } else {
-      return new SourceLocation(first.loc.lexer, first.loc.start, second.loc.end);
+      const start = first.loc.source || first.loc;
+      const end = second.loc.source || second.loc;
+      const source = (first.loc.source || second.loc.source) && start.lexer === end.lexer
+          ? new SourceLocation(start.lexer, Math.min(start.start, end.start), Math.max(start.end, end.end))
+          : undefined;
+      return new SourceLocation(first.loc.lexer, first.loc.start, second.loc.end, source);
     }
   }
 }
@@ -14459,7 +14539,10 @@ class Lexer {
       }
       return this.lex();
     }
-    return new Token(text, new SourceLocation(this, pos, this.tokenRegex.lastIndex));
+    // A control word consumes following whitespace, which is not part of its glyph.
+    const source = text.startsWith("\\") && input.slice(pos, pos + text.length) === text
+      ? new SourceLocation(this, pos, pos + text.length) : undefined;
+    return new Token(text, new SourceLocation(this, pos, this.tokenRegex.lastIndex, source));
   }
 }
 ;// ./src/Namespace.ts
@@ -15850,7 +15933,21 @@ class MacroExpander {
       return false;
     }
     this.countExpansion(1);
-    let tokens = expansion.tokens;
+    // Keep parser locations intact; rendered expansion symbols point to the call.
+    const source = topToken.loc && (topToken.loc.source || topToken.loc);
+    let tokens = expansion.tokens.map(token => {
+        const loc = token.loc;
+        // Function macros can return arguments already located in the caller's input.
+        if (!source || (loc && loc.lexer === source.lexer && loc.start >= source.end)) {
+            return token;
+        }
+        const mapped = new Token(token.text, loc
+            ? new SourceLocation(loc.lexer, loc.start, loc.end, source)
+            : new SourceLocation(source.lexer, source.start, source.end, source));
+        mapped.noexpand = token.noexpand;
+        mapped.treatAsRelax = token.treatAsRelax;
+        return mapped;
+    });
     const args = this.consumeArgs(expansion.numArgs, expansion.delimiters);
     if (expansion.numArgs) {
       // paste arguments in place of the placeholders
@@ -16994,7 +17091,9 @@ class Parser {
       args,
       optArgs
     } = this.parseArguments(func, funcData);
-    return this.callFunction(func, args, optArgs, token, breakOnTokenText);
+    const result = this.callFunction(func, args, optArgs, token, breakOnTokenText);
+    if (result.loc == null) result.loc = token.loc;
+    return result;
   }
 
   /**
diff --git a/dist/katex.mjs b/dist/katex.mjs
index 250484b791d3954163ae2320972ca011d2494df9..9ee9543576c748588eff6697997c34c4a0cc6c44 100644
--- a/dist/katex.mjs
+++ b/dist/katex.mjs
@@ -1123,6 +1123,7 @@ class SymbolNode {
     this.width = width || 0;
     this.classes = classes || [];
     this.style = style || {};
+    this.attributes = {};
     this.maxFontSize = 0;
     // Mark text from non-Latin scripts with specific classes so that we
     // can specify which fonts to use.  This allows us to render these
@@ -1143,6 +1144,9 @@ class SymbolNode {
   hasClass(className) {
     return this.classes.includes(className);
   }
+  setAttribute(attribute, value) {
+    this.attributes[attribute] = value;
+  }
   /**
    * Creates a text node or span from a symbol node. Note that a span is only
    * created if it is needed.
@@ -1162,6 +1166,10 @@ class SymbolNode {
       span = span || document.createElement("span");
       span.style[key] = this.style[key];
     }
+    for (var attr of Object.keys(this.attributes)) {
+      span = span || document.createElement("span");
+      span.setAttribute(attr, this.attributes[attr]);
+    }
     if (span) {
       span.appendChild(node);
       return span;
@@ -1194,6 +1202,10 @@ class SymbolNode {
       needsSpan = true;
       markup += " style=\"" + escape(styles) + "\"";
     }
+    for (var attr of Object.keys(this.attributes)) {
+      needsSpan = true;
+      markup += " " + attr + "=\"" + escape(this.attributes[attr]) + "\"";
+    }
     var escaped = escape(this.text);
     if (needsSpan) {
       markup += ">";
@@ -4755,6 +4767,17 @@ var makeOrd = function makeOrd(group, options, type) {
  * correction so we don't lose spacing between combined glyphs.
  */
 var canCombine = (prev, next) => {
+  // Only combine mapped text when its original character boundaries survive.
+  const start = prev.attributes["data-loc-start"];
+  const end = prev.attributes["data-loc-end"];
+  const nextStart = next.attributes["data-loc-start"];
+  const nextEnd = next.attributes["data-loc-end"];
+  if (start != null || nextStart != null) {
+      if (end !== nextStart || +end - +start !== prev.text.length
+          || +nextEnd - +nextStart !== next.text.length) {
+          return false;
+      }
+  }
   if (createClass(prev.classes) !== createClass(next.classes) || prev.skew !== next.skew || prev.maxFontSize !== next.maxFontSize || prev.italic !== 0 && prev.hasClass("mathnormal")) {
     return false;
   }
@@ -4787,6 +4810,9 @@ var tryCombineChars = chars => {
     var prev = chars[i];
     var next = chars[i + 1];
     if (prev instanceof SymbolNode && next instanceof SymbolNode && canCombine(prev, next)) {
+      if (next.attributes["data-loc-end"] != null) {
+        prev.setAttribute("data-loc-end", next.attributes["data-loc-end"]);
+      }
       prev.text += next.text;
       prev.height = Math.max(prev.height, next.height);
       prev.depth = Math.max(prev.depth, next.depth);
@@ -5510,6 +5536,39 @@ var makeNullDelimiter = function makeNullDelimiter(options, classes) {
   var moreClasses = ["nulldelimiter"].concat(options.baseSizingClasses());
   return makeSpan(classes.concat(moreClasses));
 };
+/**
+ * Recursively scan a virtual DOM node for data-loc-start / data-loc-end
+ * attributes and return the merged [min, max] range, or null if none found.
+ */
+function _inheritLocRange(node) {
+  var minStart = Infinity;
+  var maxEnd = -Infinity;
+  var stack = [node];
+  while (stack.length) {
+    var cur = stack.pop();
+    if (cur.attributes) {
+      var s = cur.attributes["data-loc-start"];
+      var e = cur.attributes["data-loc-end"];
+      if (s != null) {
+        var sv = +s;
+        if (sv < minStart) minStart = sv;
+      }
+      if (e != null) {
+        var ev = +e;
+        if (ev > maxEnd) maxEnd = ev;
+      }
+    }
+    if (cur.children) {
+      for (var i = cur.children.length - 1; i >= 0; i--) {
+        stack.push(cur.children[i]);
+      }
+    }
+  }
+  if (minStart !== Infinity && maxEnd !== -Infinity) {
+    return [minStart, maxEnd];
+  }
+  return null;
+}
 /**
  * buildGroup is the function that takes a group and calls the correct groupType
  * function for it. It also handles the interaction of size and style changes
@@ -5531,6 +5590,20 @@ var buildGroup$1 = function buildGroup(group, options, baseOptions) {
       groupNode.height *= multiplier;
       groupNode.depth *= multiplier;
     }
+    if ("setAttribute" in groupNode && typeof groupNode.setAttribute === "function" &&
+        (!groupNode.attributes || groupNode.attributes["data-loc-start"] == null)) {
+      const loc = group.loc && (group.loc.source || group.loc);
+      if (loc != null) {
+        groupNode.setAttribute("data-loc-start", String(loc.start));
+        groupNode.setAttribute("data-loc-end", String(loc.end));
+      } else {
+        var range = _inheritLocRange(groupNode);
+        if (range) {
+          groupNode.setAttribute("data-loc-start", String(range[0]));
+          groupNode.setAttribute("data-loc-end", String(range[1]));
+        }
+      }
+    }
     return groupNode;
   } else {
     throw new ParseError("Got group of unknown type: '" + group.type + "'");
@@ -9256,10 +9329,11 @@ class SourceLocation {
   // Lexer holding the input string.
   // Start offset, zero-based inclusive.
   // End offset, zero-based exclusive.
-  constructor(lexer, start, end) {
+  constructor(lexer, start, end, source) {
     this.lexer = lexer;
     this.start = start;
     this.end = end;
+    this.source = source;
   }
   /**
    * Merges two `SourceLocation`s from location providers, given they are
@@ -9275,7 +9349,12 @@ class SourceLocation {
     } else if (!first || !first.loc || !second.loc || first.loc.lexer !== second.loc.lexer) {
       return null;
     } else {
-      return new SourceLocation(first.loc.lexer, first.loc.start, second.loc.end);
+      const start = first.loc.source || first.loc;
+      const end = second.loc.source || second.loc;
+      const source = (first.loc.source || second.loc.source) && start.lexer === end.lexer
+          ? new SourceLocation(start.lexer, Math.min(start.start, end.start), Math.max(start.end, end.end))
+          : undefined;
+      return new SourceLocation(first.loc.lexer, first.loc.start, second.loc.end, source);
     }
   }
 }
@@ -13376,7 +13455,10 @@ class Lexer {
       }
       return this.lex();
     }
-    return new Token(text, new SourceLocation(this, pos, this.tokenRegex.lastIndex));
+    // A control word consumes following whitespace, which is not part of its glyph.
+    const source = text.startsWith("\\") && input.slice(pos, pos + text.length) === text
+      ? new SourceLocation(this, pos, pos + text.length) : undefined;
+    return new Token(text, new SourceLocation(this, pos, this.tokenRegex.lastIndex, source));
   }
 }
 
@@ -14656,7 +14738,21 @@ class MacroExpander {
       return false;
     }
     this.countExpansion(1);
-    var tokens = expansion.tokens;
+    // Keep parser locations intact; rendered expansion symbols point to the call.
+    const source = topToken.loc && (topToken.loc.source || topToken.loc);
+    let tokens = expansion.tokens.map(token => {
+        const loc = token.loc;
+        // Function macros can return arguments already located in the caller's input.
+        if (!source || (loc && loc.lexer === source.lexer && loc.start >= source.end)) {
+            return token;
+        }
+        const mapped = new Token(token.text, loc
+            ? new SourceLocation(loc.lexer, loc.start, loc.end, source)
+            : new SourceLocation(source.lexer, source.start, source.end, source));
+        mapped.noexpand = token.noexpand;
+        mapped.treatAsRelax = token.treatAsRelax;
+        return mapped;
+    });
     var args = this.consumeArgs(expansion.numArgs, expansion.delimiters);
     if (expansion.numArgs) {
       // paste arguments in place of the placeholders
@@ -15751,7 +15847,9 @@ class Parser {
       args,
       optArgs
     } = this.parseArguments(func, funcData);
-    return this.callFunction(func, args, optArgs, token, breakOnTokenText);
+    const result = this.callFunction(func, args, optArgs, token, breakOnTokenText);
+    if (result.loc == null) result.loc = token.loc;
+    return result;
   }
   /**
    * Call a function handler with a suitable context and arguments.
diff --git a/src/Lexer.ts b/src/Lexer.ts
index ad3ac38a442656c7b5d6dfed80b0da97c77da8dc..2bc58b454eb38210014d3d7345dcf45b9727ef4d 100644
--- a/src/Lexer.ts
+++ b/src/Lexer.ts
@@ -115,7 +115,10 @@ export default class Lexer implements LexerInterface {
             return this.lex();
         }
 
+        // A control word consumes following whitespace, which is not part of its glyph.
+        const source = text.startsWith("\\") && input.slice(pos, pos + text.length) === text
+            ? new SourceLocation(this, pos, pos + text.length) : undefined;
         return new Token(text, new SourceLocation(this, pos,
-            this.tokenRegex.lastIndex));
+            this.tokenRegex.lastIndex, source));
     }
 }
diff --git a/src/MacroExpander.ts b/src/MacroExpander.ts
index 548724adae035e2192dc92ca96c2fdbffdef5f49..9d51ecf55248fb87449d9583df0cf13540c3362a 100644
--- a/src/MacroExpander.ts
+++ b/src/MacroExpander.ts
@@ -289,7 +289,21 @@ export default class MacroExpander implements MacroContextInterface {
             return false;
         }
         this.countExpansion(1);
-        let tokens = expansion.tokens;
+        // Keep parser locations intact; rendered expansion symbols point to the call.
+        const source = topToken.loc && (topToken.loc.source || topToken.loc);
+        let tokens = expansion.tokens.map(token => {
+            const loc = token.loc;
+            // Function macros can return arguments already located in the caller's input.
+            if (!source || (loc && loc.lexer === source.lexer && loc.start >= source.end)) {
+                return token;
+            }
+            const mapped = new Token(token.text, loc
+                ? new SourceLocation(loc.lexer, loc.start, loc.end, source)
+                : new SourceLocation(source.lexer, source.start, source.end, source));
+            mapped.noexpand = token.noexpand;
+            mapped.treatAsRelax = token.treatAsRelax;
+            return mapped;
+        });
         const args = this.consumeArgs(expansion.numArgs, expansion.delimiters);
         if (expansion.numArgs) {
             // paste arguments in place of the placeholders
diff --git a/src/Parser.ts b/src/Parser.ts
index 169b2c2ae13a3f15d77cbfce69e64081e3c32174..0577583cfdf17d2ea1add7bb77520493efb656b4 100644
--- a/src/Parser.ts
+++ b/src/Parser.ts
@@ -489,7 +489,9 @@ export default class Parser {
         }
 
         const {args, optArgs} = this.parseArguments(func, funcData);
-        return this.callFunction(func, args, optArgs, token, breakOnTokenText);
+        const result = this.callFunction(func, args, optArgs, token, breakOnTokenText);
+        if (result.loc == null) result.loc = token.loc;
+        return result;
     }
 
     /**
diff --git a/src/SourceLocation.ts b/src/SourceLocation.ts
index 709748457824acdf8f257c4f86d18f96400b5f60..ce8ae0649d9494fa67686f60122c6372a2215084 100644
--- a/src/SourceLocation.ts
+++ b/src/SourceLocation.ts
@@ -10,10 +10,13 @@ export default class SourceLocation {
     readonly start: number;         // Start offset, zero-based inclusive.
     readonly end: number;           // End offset, zero-based exclusive.
 
-    constructor(lexer: LexerInterface, start: number, end: number) {
+    readonly source?: SourceLocation;
+
+    constructor(lexer: LexerInterface, start: number, end: number, source?: SourceLocation) {
         this.lexer = lexer;
         this.start = start;
         this.end = end;
+        this.source = source;
     }
 
     /**
@@ -34,8 +37,13 @@ export default class SourceLocation {
                    first.loc.lexer !== second.loc.lexer) {
             return null;
         } else {
+            const start = first.loc.source || first.loc;
+            const end = second.loc.source || second.loc;
+            const source = (first.loc.source || second.loc.source) && start.lexer === end.lexer
+                ? new SourceLocation(start.lexer, Math.min(start.start, end.start), Math.max(start.end, end.end))
+                : undefined;
             return new SourceLocation(
-                    first.loc.lexer, first.loc.start, second.loc.end);
+                    first.loc.lexer, first.loc.start, second.loc.end, source);
         }
     }
 }
diff --git a/src/buildCommon.ts b/src/buildCommon.ts
index 80250df1837fb5c52306f98e2b6191b36f74130a..c28a140975ea5114d4e0fb994c49fab368af573d 100644
--- a/src/buildCommon.ts
+++ b/src/buildCommon.ts
@@ -251,6 +251,17 @@ export const makeOrd = function<NODETYPE extends "spacing" | "mathord" | "textor
  * correction so we don't lose spacing between combined glyphs.
  */
 const canCombine = (prev: SymbolNode, next: SymbolNode) => {
+    // Only combine mapped text when its original character boundaries survive.
+    const start = prev.attributes["data-loc-start"];
+    const end = prev.attributes["data-loc-end"];
+    const nextStart = next.attributes["data-loc-start"];
+    const nextEnd = next.attributes["data-loc-end"];
+    if (start != null || nextStart != null) {
+        if (end !== nextStart || +end - +start !== prev.text.length
+            || +nextEnd - +nextStart !== next.text.length) {
+            return false;
+        }
+    }
     if (createClass(prev.classes) !== createClass(next.classes)
         || prev.skew !== next.skew
         || prev.maxFontSize !== next.maxFontSize
@@ -294,6 +305,9 @@ export const tryCombineChars = (chars: HtmlDomNode[]): HtmlDomNode[] => {
             && next instanceof SymbolNode
             && canCombine(prev, next)) {
 
+            if (next.attributes["data-loc-end"] != null) {
+                prev.setAttribute("data-loc-end", next.attributes["data-loc-end"]);
+            }
             prev.text += next.text;
             prev.height = Math.max(prev.height, next.height);
             prev.depth = Math.max(prev.depth, next.depth);
diff --git a/src/buildHTML.ts b/src/buildHTML.ts
index 24ef95b02bf9506aebfa339b7f305d1794589f23..f3d55674daf3b1e9c834cfaf07b72b583dad419b 100644
--- a/src/buildHTML.ts
+++ b/src/buildHTML.ts
@@ -251,6 +251,42 @@ export const makeNullDelimiter = function(
     return makeSpan(classes.concat(moreClasses));
 };
 
+/**
+ * Recursively scan a virtual DOM node for data-loc-start / data-loc-end
+ * attributes and return the merged [min, max] range, or null if none found.
+ */
+function inheritLocRange(
+    node: HtmlDomNode,
+): [number, number] | null {
+    let minStart = Infinity;
+    let maxEnd = -Infinity;
+    const stack: HtmlDomNode[] = [node];
+    while (stack.length) {
+        const cur = stack.pop()!;
+        if ("attributes" in cur && (cur as any).attributes) {
+            const s = (cur as any).attributes["data-loc-start"];
+            const e = (cur as any).attributes["data-loc-end"];
+            if (s != null) {
+                const sv = +s;
+                if (sv < minStart) minStart = sv;
+            }
+            if (e != null) {
+                const ev = +e;
+                if (ev > maxEnd) maxEnd = ev;
+            }
+        }
+        if ("children" in cur && Array.isArray((cur as any).children)) {
+            for (let i = (cur as any).children.length - 1; i >= 0; i--) {
+                stack.push((cur as any).children[i]);
+            }
+        }
+    }
+    if (minStart !== Infinity && maxEnd !== -Infinity) {
+        return [minStart, maxEnd];
+    }
+    return null;
+}
+
 /**
  * buildGroup is the function that takes a group and calls the correct groupType
  * function for it. It also handles the interaction of size and style changes
@@ -283,6 +319,26 @@ export const buildGroup = function(
             groupNode.depth *= multiplier;
         }
 
+        if (
+            "setAttribute" in groupNode &&
+            typeof groupNode.setAttribute === "function" &&
+            (!("attributes" in groupNode) || groupNode.attributes["data-loc-start"] == null)
+        ) {
+            const loc = group.loc && (group.loc.source || group.loc);
+            if (loc != null) {
+                groupNode.setAttribute("data-loc-start", String(loc.start));
+                groupNode.setAttribute("data-loc-end", String(loc.end));
+            } else {
+                const range = inheritLocRange(groupNode);
+                if (range) {
+                    groupNode.setAttribute(
+                        "data-loc-start", String(range[0]));
+                    groupNode.setAttribute(
+                        "data-loc-end", String(range[1]));
+                }
+            }
+        }
+
         return groupNode;
     } else {
         throw new ParseError(
diff --git a/src/domTree.ts b/src/domTree.ts
index 8d60b661b65abd83c64885b1e482e47ae4739c4c..2d3bf7f4ea502c5abb70993069f1527e3c8825df 100644
--- a/src/domTree.ts
+++ b/src/domTree.ts
@@ -369,6 +369,7 @@ export class SymbolNode implements HtmlDomNode {
     width: number;
     maxFontSize: number;
     classes: string[];
+    attributes: Record<string, string>;
     style: CssStyle;
 
     constructor(
@@ -389,6 +390,7 @@ export class SymbolNode implements HtmlDomNode {
         this.width = width || 0;
         this.classes = classes || [];
         this.style = style || {};
+        this.attributes = {};
         this.maxFontSize = 0;
 
         // Mark text from non-Latin scripts with specific classes so that we
@@ -412,6 +414,10 @@ export class SymbolNode implements HtmlDomNode {
         return this.classes.includes(className);
     }
 
+    setAttribute(attribute: string, value: string) {
+        this.attributes[attribute] = value;
+    }
+
     /**
      * Creates a text node or span from a symbol node. Note that a span is only
      * created if it is needed.
@@ -435,6 +441,11 @@ export class SymbolNode implements HtmlDomNode {
             (span.style as any)[key] = this.style[key];
         }
 
+        for (const attr of Object.keys(this.attributes)) {
+            span = span || document.createElement("span");
+            span.setAttribute(attr, this.attributes[attr]);
+        }
+
         if (span) {
             span.appendChild(node);
             return span;
@@ -474,6 +485,11 @@ export class SymbolNode implements HtmlDomNode {
             markup += " style=\"" + escape(styles) + "\"";
         }
 
+        for (const attr of Object.keys(this.attributes)) {
+            needsSpan = true;
+            markup += ` ${attr}="${escape(this.attributes[attr])}"`;
+        }
+
         const escaped = escape(this.text);
         if (needsSpan) {
             markup += ">";
